import { CaseDirectiveDirective } from './case-directive.directive';

describe('CaseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
